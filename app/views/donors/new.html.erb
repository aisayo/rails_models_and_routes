<h1>donors#new</h1>

<% if @donor.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@donor.errors.count, "error") %> prohibited this article from being saved:</h2>

    <ul>
      <% @donor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_with model: @donor do |f| %>
    <%= f.label "name" %>
    <%= f.text_field :name %>
    <br>
    <%= f.label "email" %>
    <%= f.text_field :email %>
    <br>
    <%= f.label "password" %>
    <%= f.password_field :password  %>
    <br>
    <%= f.submit %>

<% end %> 




